# 2.4.0

- Added mandatory academic-planning PDF preflight with contract, fixed-case, page, font, evidence, client-boundary and rendered-PNG checks.
- PDF export now creates PNG proofs and a `.preflight.json`; final delivery requires a second pass with `--visual-reviewed`.
- T01 is hard-bound to fixed case 01, exactly two pages, the PingFang SC CSS stack, logo, IP, six modules, course map, roadmap and AI execution area.
- Passing preflight emits the required delivery acceptance declaration.
