# 使用手册

## 先判断任务属于谁

- “个性化学业规划报告、客户学业方案、全年规划、陪跑、专业课、AI智慧学习系统、课程优先级、阶段时间轴” → `jizhi-academic-year-plan-proposal`
- “DP安心包、DP全包、作业保分、纯DP服务方案” → `dp-proposal-designer`
- “Assessment整理、DP作业量、DP最终报价、报价Excel” → `dp-product-new-customer-quote`
- “DP + 陪跑” → 年度 skill 统筹；分别调用 DP 方案与 DP 报价；范围和价格分栏呈现

## 固定生成顺序

1. 确认负责人 skill 和输出合同编号。
2. 收集会改变范围的必要输入。
3. 只读取该 skill 的入口及选定合同。
4. 调研动态的学校、课程、考核和校历信息。
5. 优先使用指令日期所在年份的学校官方信息；没有时使用最新官方信息，并保留来源网址。
6. 自动复用固定价值模块，不重新撰写。
7. 生成包含“课程与服务匹配”的初稿并完成合同级检查。
8. 方案确认后再报价；DP价格只调用报价 skill，规划价格只调用内部报价插件。
9. 有未知、估算或超边界内容时，输出：`亲爱的学业规划师，您好！此次方案生成存在【预警提示】：...`

## 稳定输入格式

```text
学校：
专业与学历：
目标学年/学期：
课程范围：
学生基础：
目标：
产品：学业规划 / DP / 混合
输出合同：T00 / T01 / T02 / T03 / T05 / D01 / D02 / D03 / Q01 / Q02
品牌：有 / 无
价格：不含 / 另出 / 合并展示
```

未指定输出合同时，skill 根据产品选择默认合同，但不得自行增加模块。

## 发布前检查

```bash
TEST_PYTHON=python3 scripts/run_tests.sh
```

然后确认：官方事实有来源；未知内容进入预警；课程与服务匹配完整；没有跨产品价格；没有API Key；PDF/Excel经过视觉检查；报价与输入指纹一致。

## Excel 与 PDF 运行环境

首次执行根目录 `./install.sh` 时会自动创建 `~/.codex/jizhi-runtime/venv`，安装 `openpyxl` 与 `pypdf`，并检测 Chrome/Edge；仅在没有本机浏览器时安装 Playwright 与隔离 Chromium。安装必须通过真实 XLSX/PDF 自检。生成脚本发现系统 Python 缺少依赖时，会自动切换到该隔离运行时。

## DP报价权限

每位需要直接报价的同事都必须在本机配置管理员私下发放的授权信息。GitHub仓库不包含共享密钥；未授权机器只能生成Assessment和待核价Excel，不能得到最终价格。

## 规划报价权限

内部同事安装单独发放的 `jizhi-planning-pricing-private` 包后，可通过年度规划 Skill 得到规划/陪跑原价、折后价、输入指纹和预警。公开 GitHub 包不包含内部价目表。标准套餐组合或服务周期不在批准矩阵时，系统必须标为待人工审核，不能自行外推。
